[project]
name = "cryper"
version = "0.1.0"
description = "Encrypt and decrypt files with plausible document names"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["cryptography"]

[project.scripts]
cryper = "cryper.cli:main"

[dependency-groups]
dev = [
    "nuitka>=4.0",
]
